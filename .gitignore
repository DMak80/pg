# Кэш graphify (пересоздаётся скиллом; готовый граф — в graphify-out/)
#arch/graphify-out/cache/

# Артефакты graphify корня (граф пересоздаётся скиллом)
graphify-out/

# Логи прогонов стенда (пересоздаются checks/*.sh)
arch/stand/logs/

# Состояние dev-flow (не артефакт задачи)
.dev-flow/

# Билд-артефакты .NET (src/PgWorker)
bin/
obj/
*.user
.vs/
artifacts/
TestResults/

# AdminPanel: фронтенд (npm/vite) — SPA-бандл в wwwroot собирается поставкой заново
node_modules/
dist/
src/AdminPanel.Api/wwwroot/

.idea/

# Секреты установки PgWorker (deploy/.env; шаблон — deploy/.env.example)
deploy/.env
