# arch/ — архитектура AdminPanel

Контракт проекта: **источник истины** о том, что строится. Код не должен
противоречить этим документам; изменение поведения = изменение документа (тем же
PR/коммитом).

Инспектируемая система — шардированные HA-кластеры PostgreSQL из репозитория
`../pg` (Patroni + etcd + бакетное шардирование). AdminPanel — read-only
инспекция **плюс четыре мутации**: создание кластера, удаление кластера, добавление/демонтаж шарда (02 §9, §9.4, §9.5, §9.6)
(декларативный provisioning в etcd, [02](02-etcd-contract.md) §9); все
остальные ключи etcd и PG панель не пишет никогда.

## Документы

| Документ | Что в нём |
|---|---|
| [01-architecture.md](01-architecture.md) | Общая архитектура: слои, проекты решения, снапшот-модель, auth, фронтенд, сборка |
| [02-etcd-contract.md](02-etcd-contract.md) | Контракт чтения etcd: ключи/префиксы, формат, модель снапшота, стратегия poll, обработка сбоев |
| [03-panels.md](03-panels.md) | Панели UI и REST API: эндпоинты, DTO, алерты |
| [04-local-stand.md](04-local-stand.md) | Собственный docker dev-стенд: etcd + шардированная PG + сид контроль-плейна + e2e-проверки |
| [roadmap/](roadmap/README.md) | Задачи на исполнение (теги `tNN-slug`, `←`-зависимости) |

Практические документы подсистем («как менять, чек-листы, грабли») — в
[`../docs/`](../docs/README.md); arch — контракт, docs — практики: при
расхождении правится arch. История задач (spec/plan) — в
`docs/superpowers/`.

## Ключевые решения (кратко)

1. **Единственное обязательное подключение к данным — etcd** (список
   endpoint'ов в настройках). Кластеры, шарды, реплики, бакеты — только из
   префиксов `/clusters/` и `/service/`. Live-пробы (Patroni REST `:8008`, SQL
   через Npgsql по DSN из etcd) — опциональные, включаются настройкой.
2. **Снапшот в памяти**: фоновый refresher каждые N секунд строит неизменяемый
   снапшот (`EtcdSnapshot`), API отдаёт его мгновенно. Запросов к etcd «на
   каждый HTTP-запрос» нет.
3. **etcd-клиент — HTTP JSON gateway** (`/v3/*`, base64-ключи), без gRPC;
   тот же механизм используют сайдкары инспектируемой системы.
4. **Backend .NET 10** по конвенциям референса `../Puzzle`: Minimal API,
   attribute-DI (`[InjectAs*]`, `[Config]`), CQRS (только queries) + `Result`,
   модульная композиция, `.slnx`, CPM.
5. **Frontend React + Vite + TypeScript** (Mantine, TanStack Query), собирается
   в `wwwroot` и раздаётся тем же ASP.NET Core; обновление — polling API
   (без WebSocket).
6. **Auth — cookie-сессия**, логин/пароль админа из настроек (без БД
   пользователей).
7. **Мутации — CQRS-команды поверх того же
   etcd-gateway (txn-клэйм имени + пакет PUT, компенсация при сбое) —
   [02](02-etcd-contract.md) §9; чтение и запись живут в одном транспорте,
   снапшот подхватывает новые ключи очередным тиком.
